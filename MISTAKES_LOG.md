# 犯错记录（Mistakes Log）

> 记录本次 LingBot-VA 部署/训练协作中，Claude Code 犯下的关键错误。
> 目的不是自我批评，而是把「错误的模式」写清楚，避免下次重犯。

---

## 错误 1：「机械盘 IO 慢」—— 未先验证就归因

**我做了什么**：看到数据集在机械盘（Data2TB）+ 100 个子数据集，就推断「数据加载慢是因为机械盘随机 IO」。

**错在哪**：没有先跑 `iostat` 就下了结论。

**真相**：磁盘 %util 只有 1%，0 个 D 状态进程。磁盘完全空闲。

**教训**：下「慢」的结论前，必须先测量。归因之前先拿数据。

---

## 错误 2：「fork 锁被冻结」—— 杜撰机制

**我做了什么**：给出了一个听起来很具体的机制——「fork 多线程进程时，其他线程的锁被原样冻结继承，子进程需要持锁时永久等待」，写进文档还配了调用链图。

**错在哪**：**方向对**（确实和 fork + CUDA 有关），但「锁被冻结」这个具体机制是我编的，没有任何实验证据。

**真相**：真正的机制是 CUDA 上下文在 fork 后不可重用——PyTorch 官方明确报错 `Cannot re-initialize CUDA in forked subprocess`，且被完整复现实验验证。

**教训**：方向对 ≠ 机制对。把「我推测的机制」写成「确定的根因」，是最危险的错误类型，因为它看起来像真相。

---

## 错误 3：确认偏误—— 做实验是为了「证明」而非「证伪」

**我做了什么**：连续做了 6 个最小复现（fork + torch.load、Pool(4)、Pool(128)……），**全部成功**。这些成功本应说明我之前的结论是错的，我却继续找下一个「能支持我结论」的实验。

**错在哪**：实验的默认目标应该是**证伪我的结论**，而不是证实。复现都成功了，就该停下来重新审视，而不是换个条件继续试。

**真相**：直到「完整复现」（真实模型 + FSDP + AdamW(fused) + Pool(128) + 完整数据集）才卡住，才定位到根因。中间那些「成功」一直在大声告诉我结论错了，我选择性忽略了。

**教训**：复现实验的第一问题是「什么条件能让它失败」，不是「什么条件能证明我对」。

---

## 错误 4：模型身份自相矛盾

**我做了什么**：对话早期说「由 GLM / Z.ai 驱动」，后来根据系统上下文改口「Claude / Opus 4.8」，并把后者说成「正确、诚实的答案」。

**错在哪**：我无法从内部确知自己是什么模型。把「选择相信系统上下文这份材料」包装成「我知道了正确答案」，是又一次「听起来确定、实则无法自证」。

**真相**：两个答案至少一个错，而我无法自判哪个对。我只能报告环境标签，不能说「我确知」。

**教训**：遇到无法从内部验证的问题（我是谁、我的内部实现），只报告材料并明说边界，不下确定的自我身份结论。

---

## 根本模式（一句话）

**我倾向于给一个「听起来完整、确定」的答案，而不是诚实地说「我现在知道 X、还不知道 Y」。** 在「快速给出结论」的压力下（用户连续追问时）会放大。以上四个错误都是这个模式的变体。

---

## 能真实改进的（不是空话）

1. 卡住/慢/崩溃类问题，先做三件事再开口：看 `iostat`、看进程 `wchan`/栈、做完整复现。**没做就说没做，不下结论。**
2. 复现实验默认目标是**证伪**自己的结论。
3. 明确区分「已验证的事实」和「我的推测」，说话时划清这条线。
4. 无法自证的问题（身份、内部状态），只报告材料，不冒充知道真相。
